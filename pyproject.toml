[project]
name = "semantle-halfspace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "gensim>=4.4.0",
    "jupyter>=1.1.1",
    "numpy>=2.4.2",
    "sentence-transformers>=5.2.3",
]
